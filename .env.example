SPRING_PROFILES_ACTIVE=local

DB_URL=jdbc:postgresql://localhost:5432/blt
DB_USERNAME=blt
DB_PASSWORD=bltscret

POSTGRES_PORT=5432
POSTGRES_DB=blt
POSTGRES_USER=blt
POSTGRES_PASSWORD=bltscret

SERVER_PORT=8080

CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:*,https://localhost:*,http://127.0.0.1:*,https://127.0.0.1:*

JWT_ISSUER=blt-api
JWT_SECRET=replace-with-64-byte-base64-secret
JWT_ACCESS_TOKEN_TTL_MINUTES=60
JWT_REFRESH_TOKEN_TTL_DAYS=30
JWT_VERIFICATION_TOKEN_TTL_MINUTES=5

APPLE_ISSUER=https://appleid.apple.com
APPLE_CLIENT_ID=BLT.com
